spring.application.name=apimapainterativo

# # Configuração do banco H2 em memória
# spring.datasource.url=jdbc:h2:file:./data/data
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.show-sql=true

# # Criar e recriar as tabelas automaticamente (apenas para desenvolvimento)
# spring.jpa.hibernate.ddl-auto=update

# # # Ativar console web do H2 (http://localhost:8080/h2-console)
# # spring.h2.console.enabled=true
# # spring.h2.console.path=/h2-console

spring.datasource.url=jdbc:postgresql://localhost:5439/postgres
spring.datasource.username=postgres
spring.datasource.password=
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
